# Radiance firmware

This repo publishes only the compiled `firmware.bin` for Radiance devices to fetch over OTA.
The source code is kept elsewhere and is not published here.

To publish a new version: build the Radiance project in PlatformIO (this folder's
`firmware.bin` is updated automatically by the project's build script), then commit and
push the updated file from here.
